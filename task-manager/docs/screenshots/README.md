# Screenshots

This folder includes starter PNGs (`auth-screen.png`, `dashboard-dark.png`) for the portfolio README. They are stylized mockups, not guaranteed pixel-perfect matches of the running app. Replace them with real captures from your local or deployed build whenever you want the README to mirror the live UI exactly.

The root `README.md` embeds these files for portfolio viewers.
